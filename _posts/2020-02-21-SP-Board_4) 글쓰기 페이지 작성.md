
```
글쓰기 페이지 작성순서


1)view
2)controller
3)dto(get-set obejct/data transfer obj)/dao(interface)/impl(service)
4)xml(controller-dao/query) 


1) view : insert.jsp 

<html>

  <div>
  <form method="post" 
  action="/board_insert_form.do">
  
  <td>title</td>
  <td><input type="text" name="title"/></td>
  
  <td>contents</td>
  <td><textarea name="content" rows="5" cols="30">
  </textarea></td>
  
  <td>
  <input type="submit" value="입력"/>
  <input type="submit" value="목록보기" 
  onclick="location.href='board_list.do'"/>
  </td>
  
  </form>
  </div>
  
</html>

---

2)controller : BoardInsertFormController.java 

import org.springframework.stereotype.Controller;

public class BoardInsertFormController extends AbstractController {

  private BoardDao boardDao;
  
  public void setBoardDao(BoardDao boardDao){
    this.boardDao = boardDao;
  }

  @RequestMapping(value="/board_insert_form.do", method=RequeustMethod.POST)
  //value= 는 생략가능
  //method=RequestMethod.POST는 POST 방식일때만 실행가능
  // -> method를 추가함으로써 get 방식일때와 post 방식 구분 및 실행가능 
  
  protected ModelAndView handleRequestInternal(HttpservletRequest arg0,
  HttpServletResponse arg1) throws Exception{ 
  
  String title = arg0.getParameter("title");
  String writer = arg0.getparameter("writer");
  String content = arg0.getParameter("content");
  
  BoardDto dto = new BoardDto();
  dto.setTitle(title);
  dto.setWriteer(writer);
  dto.setContent(content);
  
  boardDao.insert(dto);
  
  return new ModelAndView("WEB-INF/jsp/insert.jsp");
  
  
  }



}
  
 
